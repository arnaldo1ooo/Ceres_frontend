import { SituacionPipe } from './situacionIcon.pipe';

describe('SituacionPipe', () => {
  it('create an instance', () => {
    const pipe = new SituacionPipe();
    expect(pipe).toBeTruthy();
  });
});
