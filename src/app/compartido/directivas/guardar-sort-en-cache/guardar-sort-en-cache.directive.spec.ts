import { GuardarSortEnCacheDirective } from './guardar-sort-en-cache.directive';

describe('GuardarSortEnCacheDirective', () => {
  it('should create an instance', () => {
    const directive = new GuardarSortEnCacheDirective();
    expect(directive).toBeTruthy();
  });
});
