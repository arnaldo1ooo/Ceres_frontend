import { EstadoOrdenPipe } from './estado-orden.pipe';

describe('EstadoOrdenPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoOrdenPipe();
    expect(pipe).toBeTruthy();
  });
});
